10.7.0
NEW
    - Added bag and bank slot spacing controls (X/Y) with shared icon size + spacing settings across Mainline, Classic, TBC and Mists

CHANGES
    - Updated bag and bank frame sizing to keep consistent right-side padding independent from slot spacing
    - Improved Scenario tracker update handling: multiple rapid event triggers are now merged into a single layout refresh per frame to prevent duplicate updates and reduce UI jitter
    - Improved Quest tracker update handling: UpdateLayout and PartialUpdate are now queued and merged per frame to avoid duplicate refreshes and reduce UI jitter
    - Improved objective notification text formatting: progress values like 2/12 are now kept visible for world and bonus objectives
    - Improved objective notification visuals: smoother icon/progress transitions plus hover blend feedback for clearer focus
    - Added a Retail-only "Private Auras" toggle for grid debuffs in raid layouts (including maintank and party grid)
    - Updated bag and bank settings (Mainline/Classic/TBC/Mists): replaced Compact Icons toggle with a shared icon-size slider and centralized bag item size normalization/configuration

FIXES
    - Fixed a chat formatting error that could trigger "invalid option in `format`" when messages contained percent signs (%)

10.6.2
FIXES
    - Temporarily disabled Grid aura timers

10.6.1
FIXES
    - Removed scenario debug print output
    - Fixed Unit Frame Lua error for non-player classes

CHANGES
    - Updated scenario handling to use the current API flow

10.6.0
NEW
    - Added class power anchor modes: Default, Center, Left and Right
    - Added class power settings: anchor X offset, anchor Y offset and custom resource bar gap
    - Added custom resource bar side option: Auto, Left, Right
    - Added minimap setting to always show the AddOn flyout button
    - Added minimap behavior to show a single AddOn button directly when flyout is not forced
    - Added setting to animate micro menu notification icons

CHANGES
    - Added EN/DE localization and descriptions for the new class power settings
    - Refactored class power anchor handling to use shared helpers
    - Improved class power sub-frame alignment for non-default anchors
    - Improved Demon Hunter and Brewmaster defaultResourceBar anchoring
    - Improved overlap handling for custom resource bars in CENTER mode
    - Improved Warlock shard and shardFragment anchoring with side settings
    - Mail skin layout now switches between compact and expanded widths based on visible right pane content
    - Refactored mail compose/open handlers and cleanup helpers to reduce duplicated UI logic
    - Moved "Sell junk automatically" from bag-specific menus to General settings near Auto Repair
    - Centralized auto-sell junk initialization to run SetupVendorJunk once globally
    - Improved Settings Overview Credits layout with section icons and two-column names
    - Improved Settings Overview changelog scroll growth behavior with smooth post-drag animation

FIXES
    - Fixed mail inbox bottom button vertical alignment
    - Fixed mail open view scroll area sizing and scrollbar spacing
    - Fixed mail separator visibility and sizing in compact/expanded modes
    - Fixed tooltip taint from comparing secret class text values (classLine:GetText)
    - Fixed scenario event timer showing in excluded zones (for example Stormarion Assault area)

10.5.1
FIXES
    - Buggrabber integration
    - Improved objective notification text formatting: progress values like 2/12 are now kept visible for world and bonus objectives
